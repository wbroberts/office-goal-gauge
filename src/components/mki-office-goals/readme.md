# office-goals

Pass in data as an array of numbers for the number of sales per day (compounded towards the goal). The component will convert it from a string into the array to pass to the chart.

<!-- Auto Generated Below -->

## Properties

| Property | Attribute | Description | Type     |
| -------- | --------- | ----------- | -------- |
| `color`  | `color`   |             | `string` |
| `data`   | `data`    |             | `string` |
| `goal`   | `goal`    |             | `number` |

---

_Built with [StencilJS](https://stenciljs.com/)_
